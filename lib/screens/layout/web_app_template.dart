import 'package:flutter/material.dart';

class WebAppTemplate extends StatefulWidget {
  const WebAppTemplate({super.key});

  @override
  State<WebAppTemplate> createState() => _WebAppTemplateState();
}

class _WebAppTemplateState extends State<WebAppTemplate> {
  @override
  Widget build(BuildContext context) {
    return const Text("Web is currently not supported");
  }
}
